import { cn } from '@/lib/utils';

export const logo = (
  <span
    className={cn(
      'bg-gradient-to-br from-purple-600 to-purple-700 bg-clip-text font-bold text-transparent'
    )}
  >
    Sabkobazzar
  </span>
);
