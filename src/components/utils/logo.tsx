import { cn } from '@/lib/utils';

export const logo = (
  <span
    className={cn(
      'bg-gradient-to-br from-violet-500 to-purple-500 bg-clip-text font-bold text-transparent'
    )}
  >
    Sabkobazzar
  </span>
);
